---
origin: "<original title>"
source_path: "<original path or URL>"
captured: "<% tp.date.now("YYYY-MM-DD") %>"
summary: "<one-line summary>"
type: "<document | transcript | export | note | other>"
domain: "<best-fit domain>"
---

[Full extracted source content. Preserve substance over compression.]

<!--
Written by `vault ingest` (or `scripts/copy_markdown_to_raw.py` for Markdown sources). Filename: memory/raw/YYYY/MM/YYYY-MM-DD-<slug>.md. Immutable once written; never edit after creation. Conversational transcripts go through `vault distil-transcript` instead, which uses a more detailed structure.
-->
